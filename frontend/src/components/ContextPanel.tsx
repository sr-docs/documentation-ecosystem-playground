import '../styles/ContextPanel.css'

export default function ContextPanel() {
  return (
    <section className="context-panel">
      <div className="context-empty">Select a workflow stage to begin</div>
    </section>
  )
}
